#include <stdlib.h>
#include <stdio.h>


/**encrypted.txt has fixed format
* v_path is the path of file
* v_str must ba char array of 64
* retuen 0:succeed otherwise failed
*/

int getEncryptedString(const char* v_path,char* v_str);
